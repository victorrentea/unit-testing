


#spring.datasource.url=jdbc:h2:mem:test
#to inspect all the SQL sent to my DB i will use a JDBC proxy driver called p6spy
spring.datasource.url=jdbc:p6spy:h2:mem:test
spring.datasource.driver-class-name=com.p6spy.engine.spy.P6SpyDriver


safety.service.url.base=http://localhost:${wiremock.server.port}
email.sender.url=http://localhost:${wiremock.server.port}

spring.kafka.producer.bootstrap-servers=${spring.embedded.kafka.brokers}
spring.kafka.consumer.bootstrap-servers=${spring.embedded.kafka.brokers}
# avoid losing messages sent before listener connects
spring.kafka.consumer.auto-offset-reset=earliest
kafka.enabled=true

