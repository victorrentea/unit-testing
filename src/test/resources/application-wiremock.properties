#set the target URL of the external API to the random port
# assigned to WireMock by @AutoConfigureWireMock(port = 0)
safety.service.url.base=http://localhost:${wiremock.server.port}
email.sender.url=http://localhost:${wiremock.server.port}