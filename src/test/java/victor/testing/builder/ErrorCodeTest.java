package victor.testing.builder;

import org.junit.jupiter.api.Test;

class ErrorCodeTest {
   @Test
   void test() {
//      for (limba: limbi)
//      for (ErrorCode value : ErrorCode.values()) {
//         messageSource.get(value.name()); ca nu crapa
//      }
   }

}